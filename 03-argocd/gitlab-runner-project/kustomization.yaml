apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gitlab-runner-project

resources:
  - base/rbac.yaml
  - base/configmap.yaml
  - base/deployment.yaml

commonLabels:
  app: gitlab-runner-project
  environment: project
  managed-by: argocd
